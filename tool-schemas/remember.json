{
  "name": "remember",
  "description": "Store a new memory for long-term recall. Use for facts, preferences, decisions.",
  "parameters": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "The memory text to store"
      },
      "type": {
        "type": "string",
        "description": "Memory type: fact, preference, decision, event"
      }
    },
    "required": ["text"]
  }
}
