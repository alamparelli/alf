{
  "name": "forget",
  "description": "Delete one or more memories by ID. Use recall first to find IDs.",
  "parameters": {
    "type": "object",
    "properties": {
      "ids": {
        "type": "array",
        "items": { "type": "integer" },
        "description": "Memory IDs to delete"
      }
    },
    "required": ["ids"],
    "x-positional": ["ids"]
  }
}
