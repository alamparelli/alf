{
  "name": "schedule",
  "description": "Manage scheduled jobs: create, list, delete, update. Supports cron schedules, one-shot times, LLM prompts, bash commands, and reminders.",
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": ["create", "list", "delete", "update"],
        "description": "Action to perform"
      },
      "id": {
        "type": "string",
        "description": "Job ID (required for delete/update)"
      },
      "name": {
        "type": "string",
        "description": "Job name (required for create)"
      },
      "schedule": {
        "type": "string",
        "description": "Cron expression (e.g. '0 0 9 * * 1-5') or RFC3339 one-shot time (required for create)"
      },
      "tier": {
        "type": "string",
        "description": "LLM tier (haiku, sonnet, opus) or 'direct' for bash commands"
      },
      "prompt": {
        "type": "string",
        "description": "Prompt for LLM tiers"
      },
      "command": {
        "type": "string",
        "description": "Bash command for direct tier"
      },
      "message": {
        "type": "string",
        "description": "Direct push notification text (no LLM, no command - mutually exclusive with prompt/command)"
      },
      "output": {
        "type": "string",
        "enum": ["chat", "file", "both", "silent"],
        "description": "Output destination (default: chat). 'chat' notifies all available channels (Telegram, Control Center)."
      },
      "timeout": {
        "type": "string",
        "description": "Execution timeout (e.g. 5m, 10m, 1h). Defaults: direct=2m, LLM=5m"
      },
      "skills": {
        "type": "string",
        "description": "Comma-separated skill names (LLM jobs only)"
      },
      "enabled": {
        "type": "string",
        "enum": ["true", "false"],
        "description": "Enable/disable job (for update)"
      },
      "user_only": {
        "type": "boolean",
        "description": "List only user-created jobs (not system/managed jobs)"
      }
    },
    "required": ["action"],
    "x-positional": ["action", "id"]
  }
}
