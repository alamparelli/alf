{
  "name": "remember",
  "description": "Store a new memory for long-term recall. Use for facts, preferences, decisions.",
  "parameters": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "The memory text to store"
      },
      "type": {
        "type": "string",
        "enum": ["fact", "preference", "decision", "event"],
        "description": "Memory type (default: fact)"
      }
    },
    "required": ["text"],
    "x-positional": ["text"]
  }
}
