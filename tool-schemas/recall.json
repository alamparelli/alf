{
  "name": "recall",
  "description": "Search long-term memory by semantic similarity. Returns relevant memories with IDs.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Search query to find relevant memories"
      },
      "limit": {
        "type": "integer",
        "description": "Max number of results (default 5)"
      }
    },
    "required": ["query"],
    "x-positional": ["query"]
  }
}
